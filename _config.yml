title: Pensieve
author: Ícar
email: personal@icarns.xyz
description: > # this means to ignore newlines until "baseurl:"
  Pensieve: a small corner of the Internet.
baseurl: "/"

# social links
twitter_username: IcarNinS # DO NOT include the @ character, or else the build will fail!
github_username:  juxuanu # DO NOT include the @ character, or else the build will fail!

show_excerpts: false # set to false to remove excerpts on the homepage

remote_theme: tareqdandachi/jekyll-shell-theme
plugins:
  - jekyll-remote-theme # add this line to the plugins list if you already have one
  - jekyll-feed
  - jemoji

footer-links:
  - rss: text

header_pages:
  - index.html
